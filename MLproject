name: tutorial

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      alpha: {type: float, default: 1}
      l1_ratio: {type: float, default: 0.1}
    command: "dvc pull"
    command: "python /workdata/mlflow/examples/sklearn_elasticnet_wine/train.py {alpha} {l1_ratio}"
